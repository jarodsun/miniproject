# 城市数据 HTML 生成系统需求说明

## 项目概述

基于 `pcas.json` 文件中的中国城市、区县、街道数据，按照拼音建立文件夹结构，并生成对应的 HTML 文件。

## 数据源

- **数据文件**: `pcas.json`
- **数据结构**: 
  ```
  省份/直辖市
    └── 城市（普通省份）或 市辖区（直辖市占位）
        └── 区县
            └── 街道列表
  ```

### 数据结构说明

1. **四个直辖市**（北京市、天津市、上海市、重庆市）：
   - 层级结构：**市 -> 区 -> 街道**
   - 数据中的"市辖区"是占位符，**需要跳过这一层**
   - 实际路径：`{直辖市拼音}/{区拼音}/{街道拼音}/index.html`

2. **其他省份**：
   - 层级结构：**省 -> 市 -> 区 -> 街道**
   - 实际路径：`{省拼音}/{市拼音}/{区拼音}/{街道拼音}/index.html`

3. **港澳台**（香港、澳门、台湾）：
   - 数据来源：可与大陆数据合并，或使用单独数据文件（如 `HK-MO-TW.json`）
   - **命名规则**：香港、澳门、台湾**使用英文**（不用拼音）；其**下级区域**若有国际通用/官方英文则用英文，找不到专用英文则继续使用拼音。
   - **香港特别行政区**：层级为 特区 → 区域（香港岛/九龙/新界）→ 区（18 区），无街道级；区域与区均有官方英文（见下文「港澳台特殊处理」）。
   - **澳门特别行政区**：层级为 特区 → 区域（澳门半岛/澳门外岛）→ 堂区，无街道级；堂区有葡文/英文专用名。
   - **台湾省**：层级为 省 → 市/县 → 区/乡镇，与大陆「省→市→区→街道」类似；市/县多有官方英文，区/乡镇无通用英文时用拼音。
   - 在**首页/索引页**中，港澳台作为独立分组展示，与「直辖市」「华北地区」等并列

## 功能需求

### 1. 文件夹结构生成

#### 1.1 四个直辖市（北京市、天津市、上海市、重庆市）

**层级结构**：市 -> 区 -> 街道（跳过"市辖区"占位层）

```
output/
  └── {直辖市拼音}/
      ├── index.html                    # 直辖市级别HTML
      └── {区拼音}/
          ├── index.html                # 区级别HTML
          └── {街道拼音}/
              └── index.html            # 街道级别HTML
```

**示例（北京市）**:
```
output/
  └── beijingshi/
      ├── index.html                    # 北京市首页
      └── dongchengqu/
          ├── index.html                # 东城区首页
          └── donghuamenjiedao/
              └── index.html            # 东华门街道首页
          └── jingshanjiedao/
              └── index.html            # 景山街道首页
          └── jiaodaokoujiedao/
              └── index.html            # 交道口街道首页
      └── xichengqu/
          ├── index.html                # 西城区首页
          └── xichanganjiejiedao/
              └── index.html            # 西长安街街道首页
          └── ...
```

#### 1.2 其他省份

**层级结构**：省 -> 市 -> 区 -> 街道

```
output/
  └── {省拼音}/
      ├── index.html                    # 省级别HTML
      └── {市拼音}/
          ├── index.html                # 市级别HTML
          └── {区拼音}/
              ├── index.html            # 区级别HTML
              └── {街道拼音}/
                  └── index.html       # 街道级别HTML
```

**示例（河北省石家庄市）**:
```
output/
  └── hebeisheng/
      ├── index.html                    # 河北省首页
      └── shijiazhuangshi/
          ├── index.html                # 石家庄市首页
          └── changanqu/
              ├── index.html            # 长安区首页
              └── jianbeijiedao/
                  └── index.html        # 建北街道首页
              └── qingyuanjiedao/
                  └── index.html        # 青园街道首页
```

### 2. 拼音转换规则

- **大陆地区**：使用中文转拼音库（如 `pypinyin`）将中文名称转换为拼音；拼音格式：全小写，去除空格和特殊字符；处理多音字：使用默认读音或根据上下文选择。
- **港澳台**：香港、澳门、台湾**不使用拼音，使用英文**（Hong Kong / Macau / Taiwan）；其**下级区域**若有专用英文（如香港 18 区、澳门堂区、台湾县市）则使用英文，找不到专用英文则继续使用拼音。

### 3. HTML 文件生成

需要为**省、市、区、街道**四个层级都生成对应的 HTML 文件：

#### 3.1 四个直辖市

**直辖市级别**：
- 文件名：`index.html`
- 文件路径：`output/{直辖市拼音}/index.html`
- 内容：显示该直辖市下的所有区列表

**区级别**：
- 文件名：`index.html`
- 文件路径：`output/{直辖市拼音}/{区拼音}/index.html`
- 内容：显示该区下的所有街道列表

**街道级别**：
- 文件夹名：`{街道拼音}/`
- 文件名：`index.html`
- 文件路径：`output/{直辖市拼音}/{区拼音}/{街道拼音}/index.html`
- 内容：显示该街道的详细信息
- **注意**：跳过"市辖区"这一层

#### 3.2 其他省份

**省级别**：
- 文件名：`index.html`
- 文件路径：`output/{省拼音}/index.html`
- 内容：显示该省下的所有市列表

**市级别**：
- 文件名：`index.html`
- 文件路径：`output/{省拼音}/{市拼音}/index.html`
- 内容：显示该市下的所有区列表

**区级别**：
- 文件名：`index.html`
- 文件路径：`output/{省拼音}/{市拼音}/{区拼音}/index.html`
- 内容：显示该区下的所有街道列表

**街道级别**：
- 文件夹名：`{街道拼音}/`
- 文件名：`index.html`
- 文件路径：`output/{省拼音}/{市拼音}/{区拼音}/{街道拼音}/index.html`
- 内容：显示该街道的详细信息

### 4. HTML 模板系统

每个 HTML 文件由三个模板部分组成：

#### 4.1 Head 模板 (`head_template.html`)
- 包含 HTML 头部信息
- `<head>` 标签内容
- 可包含 meta 标签、CSS 链接等
- 所有层级共用同一个 head 模板

#### 4.2 Body 模板

根据不同的层级，可以使用不同的 body 模板：

**省级别/直辖市级别 Body 模板** (`body_province_template.html` 或 `body_municipality_template.html`)：

- **普通省份**：显示该省下的所有市列表；支持变量 `{省份名称}`、`{省份拼音}`、`{下级列表}` 等。
- **直辖市**（`body_municipality_template.html`）：
  - **左侧主内容**与首页**完全一致**：导航（面包屑：首页、当前直辖市名称，见「直辖市页面右侧侧栏规则」§1）、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与内容同首页）。
  - **右侧**为直辖市专用侧栏（见下文「直辖市页面右侧侧栏规则」），由生成脚本注入 `{直辖市右侧侧栏}`；模板还使用 `{banner}`、产品相关变量（与首页一致）。

**市级别 Body 模板** (`body_city_template.html`)：
- 显示该市下的所有区列表
- 支持变量替换：
  - `{省份名称}` - 省份名称
  - `{城市名称}` - 城市名称
  - `{省份拼音}` - 省份的拼音
  - `{城市拼音}` - 城市的拼音
  - `{下级列表}` - 区列表

**区级别 Body 模板** (`body_district_template.html`)：
- 显示该区下的所有街道列表
- 支持变量替换：
  - `{省份名称}` - 省份或直辖市名称
  - `{城市名称}` - 城市名称（直辖市时与省份名称相同）
  - `{区县名称}` - 区县名称
  - `{省份拼音}` - 省份或直辖市的拼音
  - `{城市拼音}` - 城市的拼音（直辖市时与省份拼音相同）
  - `{区县拼音}` - 区县的拼音
  - `{下级列表}` - 街道列表

**街道级别 Body 模板** (`body_street_template.html`)：
- 显示该街道的详细信息
- 支持变量替换：
  - `{省份名称}` - 省份或直辖市名称
  - `{城市名称}` - 城市名称（直辖市时与省份名称相同）
  - `{区县名称}` - 区县名称
  - `{街道名称}` - 街道名称
  - `{省份拼音}` - 省份或直辖市的拼音
  - `{城市拼音}` - 城市的拼音（直辖市时与省份拼音相同）
  - `{区县拼音}` - 区县的拼音
  - `{街道拼音}` - 街道的拼音

**变量说明**：
- 对于**四个直辖市**：`{城市名称}` = `{省份名称}`（都是直辖市名称）
- 对于**普通省份**：`{城市名称}` ≠ `{省份名称}`（城市是省份下的地级市）
- `{下级列表}` 变量需要根据实际数据动态生成列表内容

#### 4.3 Foot 模板 (`foot_template.html`)
- 包含 HTML 底部信息
- `</body>` 和 `</html>` 标签
- 可包含 JavaScript 脚本、统计代码等
- 所有层级共用同一个 foot 模板

### 5. 生成流程

1. **读取数据**
   - 加载 `pcas.json` 文件
   - 解析 JSON 数据结构

2. **遍历数据并生成 HTML**
   - 遍历所有省份/直辖市
   - **判断是否为直辖市**（北京市、天津市、上海市、重庆市）
   - **如果是直辖市**：
     - **生成直辖市级别 HTML**：`output/{直辖市拼音}/index.html`
     - 跳过"市辖区"这一层（这是占位符）
     - 直接遍历区县
     - 对于每个区县：
       - **生成区级别 HTML**：`output/{直辖市拼音}/{区拼音}/index.html`
       - 遍历该区县下的所有街道
       - 对于每个街道：
         - **创建街道文件夹**：`output/{直辖市拼音}/{区拼音}/{街道拼音}/`
         - **生成街道级别 HTML**：`output/{直辖市拼音}/{区拼音}/{街道拼音}/index.html`
   - **如果是普通省份**：
     - **生成省级别 HTML**：`output/{省拼音}/index.html`
     - 遍历每个省份下的所有城市
     - 对于每个城市：
       - **生成市级别 HTML**：`output/{省拼音}/{市拼音}/index.html`
       - 遍历该城市下的所有区县
       - 对于每个区县：
         - **生成区级别 HTML**：`output/{省拼音}/{市拼音}/{区拼音}/index.html`
         - 遍历该区县下的所有街道
         - 对于每个街道：
           - **创建街道文件夹**：`output/{省拼音}/{市拼音}/{区拼音}/{街道拼音}/`
           - **生成街道级别 HTML**：`output/{省拼音}/{市拼音}/{区拼音}/{街道拼音}/index.html`

3. **拼音转换**
   - 将省份、城市、区县、街道名称转换为拼音
   - 生成文件夹路径和文件名

4. **创建文件夹**
   - 根据拼音路径创建对应的文件夹结构
   - 确保父目录存在

5. **生成 HTML**
   - 根据层级选择对应的 body 模板：
     - 省级别/直辖市级别：使用 `body_province_template.html` 或 `body_municipality_template.html`
     - 市级别：使用 `body_city_template.html`
     - 区级别：使用 `body_district_template.html`
     - 街道级别：使用 `body_street_template.html`
   - 读取 head、对应的 body、foot 模板文件
   - 替换 body 模板中的变量（包括生成下级列表）
   - 组合三个模板生成完整 HTML
   - 写入到对应路径

## 首页地区导航规则

以下规则以模板 `cities/templates_index/index.html` 中**右侧地区导航**（sidebar）为准，生成首页索引时需按此规则输出地区列表与链接。

### 1. 导航结构

- **位置**：页面右侧侧栏（`.sidebar`），含「展开地区导航」按钮，移动端可折叠。
- **两层结构**：
  1. **产品切换**（`.sidebar-nav-list`）：云服务器、高防服务器、服务器托管、IDC、机柜；每项对应 `data-for`：`cloud`、`ddos`、`csr`、`idc`、`trusteeship`。
  2. **地区列表**（`.address-list`）：每个产品一套地区列表（如 `product-cloud`、`product-ddos` 等），当前产品对应列表加 `active`；**同一套分组顺序与内容用于所有产品**，仅链接后缀不同（见下）。

### 2. 分组顺序与标签

地区按以下**固定顺序**分为 9 组，每组左侧为标签（`.address-item-left`），右侧为链接区（`.address-item-right`）：

| 顺序 | 标签         | 说明 |
|------|--------------|------|
| 1    | 直辖市：     | 北京、天津、上海、重庆 |
| 2    | 港澳台：     | 香港、澳门、台湾（使用英文路径） |
| 3    | 华北地区：   | 河北、山西、内蒙古 |
| 4    | 东北地区：   | 辽宁、吉林、黑龙江 |
| 5    | 华东地区：   | 江苏、浙江、安徽、福建、江西、山东 |
| 6    | 华中地区：   | 河南、湖北、湖南 |
| 7    | 华南地区：   | 广东、广西、海南 |
| 8    | 西南地区：   | 四川、贵州、云南、西藏 |
| 9    | 西北地区：   | 陕西、甘肃、青海、宁夏、新疆 |

### 3. 各组展示形式

- **直辖市、港澳台**（第 1、2 组）：
  - 仅一级链接：`<dl><dt>...</dt></dl>`，`<dt>` 内为多个平级的 `<a>`。
  - 直辖市：4 个链接（北京、天津、上海、重庆），**显示名为简称**（无「市」）。
  - 港澳台：3 个链接（香港、澳门、台湾），路径用英文（见「港澳台特殊处理」）。

- **七大区域**（第 3～9 组）：
  - 省 + 下辖市：`<dl>` 内多个 `<dt>`（省名+链接），每个 `<dt>` 后跟一个 `<dd>`（该省下辖市，多个 `<a>`）。
  - 省链接指向省级页（如 `hebei/cloud.html`），市链接指向市级页（如 `shijiazhuang/cloud.html`）。

### 4. 链接格式

- 链接形式：`{path_slug}/{产品页}.html`。
- **产品页**与产品对应关系：云服务器 → `cloud.html`，高防服务器 → `ddos.html`，服务器托管 → `csr.html`，IDC → `idc.html`，机柜 → `trusteeship.html`。
- 示例：北京云服务器 `beijing/cloud.html`，河北高防 `hebei/ddos.html`，石家庄托管 `shijiazhuang/csr.html`，香港云服务器 `hong-kong/cloud.html`（港澳台用英文路径）。

### 5. 命名与 path_slug 规则

- **直辖市**：显示用简称（北京、天津、上海、重庆），path_slug 用拼音：`beijing`、`tianjin`、`shanghai`、`chongqing`（不带「市」）。
- **港澳台**：显示用中文名，path_slug 用英文：`hong-kong`、`macau`、`taiwan`（见 `HK-MO-TW_name_mapping.json`）。
- **省份**：显示用全称（如河北、山西），path_slug 为拼音（如 `hebei`、`shanxi`）；**山西与陕西**等同名须区分，模板中陕西用 `shanxi1`。
- **城市**：显示用市名（如石家庄、太原），path_slug 为拼音（如 `shijiazhuang`、`taiyuan`）；与直辖市、省份、其他市同名的，用后缀或约定区分（如台州 `taizhou1`、宿州 `suzhou1`、抚州 `fuzhou1`）。

### 6. 七大区域与省份归属

生成时需将大陆省份归入上述 7 个区域，与模板一致：

- **华北地区**：河北省、山西省、内蒙古自治区
- **东北地区**：辽宁省、吉林省、黑龙江省
- **华东地区**：江苏省、浙江省、安徽省、福建省、江西省、山东省
- **华中地区**：河南省、湖北省、湖南省
- **华南地区**：广东省、广西壮族自治区、海南省
- **西南地区**：四川省、贵州省、云南省、西藏自治区
- **西北地区**：陕西省、甘肃省、青海省、宁夏回族自治区、新疆维吾尔自治区

每个区域下，各省顺序与模板内一致；省内下辖市列表与 `pcas.json` 中该省下属城市顺序一致（或按模板中给出的市列表顺序）。

---

## 直辖市页面右侧侧栏规则

从首页点击进入**直辖市页面**（如 `beijingshi/index.html`、`shanghaishi/index.html`）后，**主内容区（body 左侧）与首页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为直辖市专用内容。以下为当前实现的直辖市页面右侧侧栏规则。

### 1. 页面与主内容

- **页面路径**：`output/{直辖市拼音}/index.html`，如 `beijingshi/index.html`（北京市）、`shanghaishi/index.html`（上海市）。
- **主内容（左侧）**：与首页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../index.html`；第二项为**当前直辖市名称**（如「北京市」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页共用同一 head、foot 模板；二级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」）。

### 2. 右侧侧栏整体结构

与首页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：北京」「返回全国」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与首页异同 |
|------|------|------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示全国 9 组，改为「本市全称 + dt 区 / dd 街道」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用直辖市**全称**（带「市」），如「北京市：」「天津市：」「上海市：」「重庆市：」。
- **`.address-item-right`（内容）**：**模仿首页**的 `<dl>` 结构——**dt** 放区（区名 + 链接），**dd** 放该区下街道（街道名 + 链接，多个用 ` | ` 分隔）：
  - **dt**：`<dt><a href="{区拼音}/index.html">{区名称}</a></dt>`（如东城区、西城区…）
  - **dd**：`<dd><a href="{区拼音}/{街道拼音}/index.html">{街道名称}</a> | …</dd>`
- 数据顺序与 `pcas.json` 中该市「市辖区」下区县、街道顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本市： + dt/dd 区·街道」内容；链接均为 `index.html`（区页、街道页），不按产品区分后缀。

### 4. 与首页侧栏的对比小结

| 项目 | 首页右侧侧栏 | 直辖市页右侧侧栏 |
|------|--------------|------------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回首页 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 9 组：直辖市、港澳台、华北～西北 | **1 块**：本市全称（如北京市：）+ dt 区、dd 街道 |
| 链接层级 | 直辖市/港澳台为一级；七大区域为省+市 | **区**（dt）+ **街道**（dd） |
| 链接路径 | `{省/市/直辖市拼音}/{产品页}.html` 等 | 区：`{区拼音}/index.html`；街道：`{区拼音}/{街道拼音}/index.html` |

### 5. 实现要点

- 侧栏数据依赖**当前直辖市**及其在 `pcas.json` 中的**区列表 + 各区街道列表**（跳过「市辖区」占位层，得到 `districts_with_streets`：`[(区名称, 区拼音, [(街道名称, 街道拼音), …]), …]`）。
- 区名、街道名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页侧栏一致，仅第二层「地区列表」的 DOM 为「本市： + dt/dd」，以便共用 CSS/JS。

---

## 直辖市「区页面」右侧侧栏规则

从直辖市页面点击进入**区页面**（如 `beijingshi/dongchengqu/index.html`）后，**主内容区（body 左侧）与首页、直辖市页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为当前区专用内容。以下为区页面的规则与逻辑。

### 1. 页面与主内容

- **页面路径**：`output/{直辖市拼音}/{区拼音}/index.html`，如 `beijingshi/dongchengqu/index.html`（北京市东城区）、`shanghaishi/huangpuqu/index.html`（上海市黄浦区）。
- **主内容（左侧）**：与首页、直辖市页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../../index.html`；第二项为直辖市名称（如「北京市」）链接到 `../index.html`；第三项为**当前区名称**（如「东城区」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页、直辖市页共用同一 head、foot 模板；三级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../css/`、`../../images/`、`../../js/`。

### 2. 右侧侧栏整体结构

与首页、直辖市页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：东城区」「返回北京市」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与直辖市页异同 |
|------|------|----------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示「本市 + dt 区 / dd 街道」，改为「本区全称 + dd 街道」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前**区全称**（带「区」），如「东城区：」「西城区：」「黄浦区：」。
- **`.address-item-right`（内容）**：仅**dd** 层级——本区下街道（街道名 + 链接，多个用 ` | ` 分隔）；**无 dt**（因当前页已是区级，无需再列区）：
  - **dd**：`<dd><a href="{街道拼音}/index.html">{街道名称}</a> | <a href="{街道拼音}/index.html">{街道名称}</a> | …</dd>`
- 数据顺序与 `pcas.json` 中该区下街道顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本区： + dd 街道」内容；链接均为 `{街道拼音}/index.html`（街道页），不按产品区分后缀。

### 4. 与直辖市页侧栏的对比小结

| 项目 | 直辖市页右侧侧栏 | 区页右侧侧栏 |
|------|------------------|--------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 1 块：本市全称 + dt 区、dd 街道 | **1 块**：本区全称（如东城区：）+ **仅 dd 街道** |
| 链接层级 | 区（dt）+ 街道（dd） | **仅街道**（dd） |
| 链接路径 | 区：`{区拼音}/index.html`；街道：`{区拼音}/{街道拼音}/index.html` | 街道：`{街道拼音}/index.html`（相对当前区页） |

### 5. 实现要点

- 侧栏数据依赖**当前区**及其在 `pcas.json` 中的**街道列表**（从直辖市 → 市辖区 → 当前区 → 街道，得到 `streets`：`[(街道名称, 街道拼音), …]`）。
- 街道名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页、直辖市页侧栏一致，仅第二层「地区列表」的 DOM 为「本区： + dd」（单层 dd，无 dt），以便共用 CSS/JS。
- 区页面为**三级页面**（首页 → 直辖市页 → 区页），资源路径相对 output 根目录为 `../../`。

---

## 普通省份「市页面」右侧侧栏规则

从省份页面点击进入**市页面**（如 `output/hebeisheng/shijiazhuangshi/index.html`、`output/guangdongsheng/guangzhoushi/index.html`）后，**主内容区（body 左侧）与首页、省份页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为当前市专用内容。以下为市页面的规则与逻辑。

### 1. 页面与主内容

- **页面路径**：`output/{省拼音}/{市拼音}/index.html`，如 `hebeisheng/shijiazhuangshi/index.html`（河北省石家庄市）、`guangdongsheng/guangzhoushi/index.html`（广东省广州市）。
- **主内容（左侧）**：与首页、省份页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../../index.html`；第二项为省份名称（如「河北省」）链接到 `../index.html`；第三项为**当前市名称**（如「石家庄市」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页、省份页共用同一 head、foot 模板；三级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../css/`、`../../images/`、`../../js/`。

### 2. 右侧侧栏整体结构

与首页、省份页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：石家庄」「返回河北省」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与省份页异同 |
|------|------|----------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示「本省 + dt 市 / dd 区县」，改为「本市全称 + dd 区县」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前**市全称**（带「市」），如「石家庄市：」「唐山市：」「广州市：」。
- **`.address-item-right`（内容）**：**模仿省页面**的 `<dl>` 结构——**dt** 放区县（区县名 + 链接），**dd** 放该区县下街道（街道名 + 链接，多个用 ` | ` 分隔）：
  - **dt**：`<dt><a href="{区县拼音}/index.html">{区县名称}</a></dt>`（如长安区、桥西区…）
  - **dd**：`<dd><a href="{区县拼音}/{街道拼音}/index.html">{街道名称}</a> | …</dd>`
- 数据顺序与 `pcas.json` 中该市下区县、街道顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本市： + dt/dd 区县·街道」内容；链接均为 `index.html`（区县页、街道页），不按产品区分后缀。

### 4. 与省份页侧栏的对比小结

| 项目 | 省份页右侧侧栏 | 市页右侧侧栏 |
|------|----------------|--------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 1 块：本省全称 + dt 市、dd 区县 | **1 块**：本市全称（如石家庄市：）+ dt 区县、dd 街道 |
| 链接层级 | 市（dt）+ 区县（dd） | **区县**（dt）+ **街道**（dd） |
| 链接路径 | 市：`{市拼音}/index.html`；区县：`{市拼音}/{区县拼音}/index.html` | 区县：`{区县拼音}/index.html`；街道：`{区县拼音}/{街道拼音}/index.html` |

### 5. 实现要点

- 侧栏数据依赖**当前市**及其在 `pcas.json` 中的**区县列表 + 各区县下街道列表**（从省 → 当前市 → 区县 → 街道，得到 `districts_with_streets`：`[(区县名称, 区县拼音, [(街道名称, 街道拼音), …]), …]`）。
- 区县名、街道名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页、省份页侧栏一致，仅第二层「地区列表」的 DOM 为「本市： + dt/dd」，以便共用 CSS/JS。
- 市页面为**三级页面**（首页 → 省页 → 市页），资源路径相对 output 根目录为 `../../`。

---

## 普通省份「区页面」右侧侧栏规则

从市页面点击进入**区页面**（如 `output/hebeisheng/shijiazhuangshi/changanqu/index.html`）后，**主内容区（body 左侧）与首页、省页、市页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为当前区专用内容。以下为区页面的规则与逻辑。

### 1. 页面与主内容

- **页面路径**：`output/{省拼音}/{市拼音}/{区拼音}/index.html`，如 `hebeisheng/shijiazhuangshi/changanqu/index.html`（河北省石家庄市长安区）。
- **主内容（左侧）**：与首页、省页、市页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../../../index.html`；第二项为省份名称（如「河北省」）链接到 `../../index.html`；第三项为市名称（如「石家庄市」）链接到 `../index.html`；第四项为**当前区名称**（如「长安区」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页、省页、市页共用同一 head、foot 模板；四级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../../css/`、`../../../images/`、`../../../js/`。

### 2. 右侧侧栏整体结构

与首页、市页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：长安区」「返回石家庄市」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与市页异同 |
|------|------|----------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示「本市 + dt 区县 / dd 街道」，改为「本区全称 + dd 街道」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前**区全称**（带「区/县」），如「长安区：」「桥西区：」「正定县：」。
- **`.address-item-right`（内容）**：仅**dd** 层级——本区下街道（街道名 + 链接，多个用 ` | ` 分隔）；**无 dt**（因当前页已是区级，无需再列区县）：
  - **dd**：`<dd><a href="{街道拼音}/index.html">{街道名称}</a> | <a href="{街道拼音}/index.html">{街道名称}</a> | …</dd>`
- 数据顺序与 `pcas.json` 中该区下街道顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本区： + dd 街道」内容；链接均为 `{街道拼音}/index.html`（街道页），不按产品区分后缀。

### 4. 与市页侧栏的对比小结

| 项目 | 市页右侧侧栏 | 区页右侧侧栏 |
|------|----------------|--------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 1 块：本市全称 + dt 区县、dd 街道 | **1 块**：本区全称（如长安区：）+ **仅 dd 街道** |
| 链接层级 | 区县（dt）+ 街道（dd） | **仅街道**（dd） |
| 链接路径 | 区县：`{区县拼音}/index.html`；街道：`{区县拼音}/{街道拼音}/index.html` | 街道：`{街道拼音}/index.html`（相对当前区页） |

### 5. 实现要点

- 侧栏数据依赖**当前区**及其在 `pcas.json` 中的**街道列表**（从省 → 市 → 当前区 → 街道，得到 `streets`：`[(街道名称, 街道拼音), …]`）。
- 街道名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页、市页侧栏一致，仅第二层「地区列表」的 DOM 为「本区： + dd」（单层 dd，无 dt），以便共用 CSS/JS。
- 区页面为**四级页面**（首页 → 省页 → 市页 → 区页），资源路径相对 output 根目录为 `../../../`。

---

## 直辖市「街道页面」右侧侧栏规则（四级页面）

从区页面点击进入**街道页面**（如 `beijingshi/dongchengqu/donghuamenjiedao/index.html`）后，**主内容区（body 左侧）与首页、直辖市页、区页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**沿用区页的「本区 + dd 街道」内容，便于在同区街道间跳转。以下为街道页面（四级页面）的规则与逻辑。

### 1. 页面与主内容

- **页面路径**：`output/{直辖市拼音}/{区拼音}/{街道拼音}/index.html`，如 `beijingshi/dongchengqu/donghuamenjiedao/index.html`（北京市东城区东华门街道）、`shanghaishi/huangpuqu/nanjingdonglujiedao/index.html`（上海市黄浦区南京东路街道）。
- **主内容（左侧）**：与首页、直辖市页、区页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../../../index.html`；第二项为直辖市名称（如「北京市」）链接到 `../../index.html`；第三项为区名称（如「东城区」）链接到 `../index.html`；第四项为**当前街道名称**（如「东华门街道」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页、直辖市页、区页共用同一 head、foot 模板；四级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../../css/`、`../../../images/`、`../../../js/`。

### 2. 右侧侧栏整体结构

与区页面侧栏**完全一致**，无「当前层级说明」块：

| 层级 | 说明 | 与区页异同 |
|------|------|------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **相同**：本区全称 + dd 街道 |

街道为叶子节点，无下级；侧栏**复用区页的「本区： + dd 街道」**结构，便于从街道页跳转到同区其他街道。

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前**区全称**（带「区」），如「东城区：」「西城区：」。
- **`.address-item-right`（内容）**：仅**dd** 层级——本区下街道（街道名 + 链接，多个用 ` | ` 分隔）；**无 dt**：
  - **dd**：`<dd><a href="{街道拼音}/index.html">{街道名称}</a> | …</dd>`（链接相对当前街道页所在目录，即区目录）。
- 数据顺序与 `pcas.json` 中该区下街道顺序一致。
- 每个产品下展示**同一套**「本区： + dd 街道」内容；链接均为 `{街道拼音}/index.html`（相对当前区目录）。

### 4. 与区页侧栏的对比小结

| 项目 | 区页右侧侧栏 | 街道页右侧侧栏 |
|------|--------------|----------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无** |
| 地区内容 | 1 块：本区全称 + dd 街道 | **1 块**：本区全称 + dd 街道，**与区页相同** |
| 链接层级 | 仅街道（dd） | **仅街道**（dd），**同上** |
| 链接路径 | 街道：`{街道拼音}/index.html`（相对区页） | 街道：`{街道拼音}/index.html`（相对区目录，即 `../` 当前街道页） |

### 5. 实现要点

- 侧栏数据与区页面相同：依赖**当前区**及其在 `pcas.json` 中的**街道列表**（`streets`：`[(街道名称, 街道拼音), …]`）。生成街道页时传入同一套 `streets`，调用与区页相同的 `build_district_sidebar_html(区名称, streets)` 即可。
- 街道名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与区页侧栏一致，DOM 为「本区： + dd」，以便共用 CSS/JS。
- 街道页面为**四级页面**（首页 → 直辖市页 → 区页 → 街道页），资源路径相对 output 根目录为 `../../../`。

---

## 港澳台页面右侧侧栏规则

从首页点击进入**港澳台页面**（如 `hong-kong/index.html`、`macau/index.html`、`taiwan/index.html`）后，**主内容区（body 左侧）与首页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为港澳台专用内容。以下为当前实现的港澳台页面右侧侧栏规则。

### 1. 页面与主内容

- **页面路径**：`output/{港澳台英文路径}/index.html`，如 `hong-kong/index.html`（香港特别行政区）、`macau/index.html`（澳门特别行政区）、`taiwan/index.html`（台湾省）。
- **主内容（左侧）**：与首页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../index.html`；第二项为**当前地区名称**（如「香港特别行政区」「澳门特别行政区」「台湾省」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页共用同一 head、foot 模板；二级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」）。

### 2. 右侧侧栏整体结构

与首页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：香港」「返回全国」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与首页异同 |
|------|------|------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示全国 9 组，改为「本地区全称 + dt 区域/市/县 / dd 区/堂区/乡镇」 |

### 3. 侧栏内容规划（第二层）

港澳台三个地区的侧栏结构因数据层级不同而有所差异：

#### 3.1 香港特别行政区

- **`.address-item-left`（标签）**：使用「香港特别行政区：」。
- **`.address-item-right`（内容）**：**模仿首页**的 `<dl>` 结构——**dt** 放区域（区域名 + 链接），**dd** 放该区域下区（区名 + 链接，多个用 ` | ` 分隔）：
  - **dt**：`<dt><a href="{区域英文路径}/index.html">{区域名称}</a></dt>`（如香港岛 Hong Kong Island、九龙 Kowloon、新界 New Territories）
  - **dd**：`<dd><a href="{区域英文路径}/{区英文路径}/index.html">{区名称}</a> | …</dd>`（如中西区 Central and Western、湾仔区 Wan Chai 等）
- 数据顺序与 `HK-MO-TW.json` 中香港「区域 → 区」顺序一致（香港岛 → 九龙 → 新界，各区按数据顺序）。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「香港特别行政区： + dt 区域 / dd 区」内容；链接均为 `index.html`（区域页、区页），不按产品区分后缀。

#### 3.2 澳门特别行政区

- **`.address-item-left`（标签）**：使用「澳门特别行政区：」。
- **`.address-item-right`（内容）**：**模仿首页**的 `<dl>` 结构——**dt** 放区域（区域名 + 链接），**dd** 放该区域下堂区（堂区名 + 链接，多个用 ` | ` 分隔）：
  - **dt**：`<dt><a href="{区域英文路径}/index.html">{区域名称}</a></dt>`（如澳门半岛 Macau Peninsula、澳门外岛 Taipa and Coloane）
  - **dd**：`<dd><a href="{区域英文路径}/{堂区英文路径}/index.html">{堂区名称}</a> | …</dd>`（如大堂区 Sé、望德堂区 São Lázaro 等）
- 数据顺序与 `HK-MO-TW.json` 中澳门「区域 → 堂区」顺序一致（澳门半岛 → 澳门外岛，各堂区按数据顺序）。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「澳门特别行政区： + dt 区域 / dd 堂区」内容；链接均为 `index.html`（区域页、堂区页），不按产品区分后缀。

#### 3.3 台湾省

- **`.address-item-left`（标签）**：使用「台湾省：」。
- **`.address-item-right`（内容）**：**模仿首页**的 `<dl>` 结构——**dt** 放市/县（市/县名 + 链接），**dd** 放该市/县下区/乡镇（区/乡镇名 + 链接，多个用 ` | ` 分隔）：
  - **dt**：`<dt><a href="{市/县英文路径}/index.html">{市/县名称}</a></dt>`（如台北市 Taipei、新北市 New Taipei、新竹县 Hsinchu County 等）
  - **dd**：`<dd><a href="{市/县英文路径}/{区/乡镇英文或拼音路径}/index.html">{区/乡镇名称}</a> | …</dd>`（如台北市大安区、新竹县竹北市等）
- 数据顺序与 `HK-MO-TW.json` 中台湾「市/县 → 区/乡镇」顺序一致（各市/县按数据顺序，各区/乡镇按数据顺序）。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「台湾省： + dt 市/县 / dd 区/乡镇」内容；链接均为 `index.html`（市/县页、区/乡镇页），不按产品区分后缀。

### 4. 与首页侧栏的对比小结

| 项目 | 首页右侧侧栏 | 港澳台页右侧侧栏 |
|------|--------------|------------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回首页 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 9 组：直辖市、港澳台、华北～西北 | **1 块**：本地区全称（如香港特别行政区：）+ dt 区域/市/县、dd 区/堂区/乡镇 |
| 链接层级 | 直辖市/港澳台为一级；七大区域为省+市 | **区域/市/县**（dt）+ **区/堂区/乡镇**（dd） |
| 链接路径 | `{省/市/直辖市拼音}/{产品页}.html` 等 | 区域/市/县：`{区域/市/县英文路径}/index.html`；区/堂区/乡镇：`{区域/市/县英文路径}/{区/堂区/乡镇英文或拼音路径}/index.html` |

### 5. 实现要点

- 侧栏数据依赖**当前港澳台地区**及其在 `HK-MO-TW.json` 中的**下级区域/市/县列表 + 各区/堂区/乡镇列表**：
  - **香港**：从 `HK-MO-TW.json` 获取区域 → 区结构，得到 `regions_with_districts`：`[(区域名称, 区域英文路径, [(区名称, 区英文路径), …]), …]`
  - **澳门**：从 `HK-MO-TW.json` 获取区域 → 堂区结构，得到 `regions_with_parishes`：`[(区域名称, 区域英文路径, [(堂区名称, 堂区英文路径), …]), …]`
  - **台湾**：从 `HK-MO-TW.json` 获取市/县 → 区/乡镇结构，得到 `cities_counties_with_districts`：`[(市/县名称, 市/县英文路径, [(区/乡镇名称, 区/乡镇英文或拼音路径), …]), …]`
- 区域名、市/县名、区名、堂区名、乡镇名转 path_slug 规则：
  - **有专用英文**：使用 `HK-MO-TW_name_mapping.json` 中的 `path_slug`（英文转小写、空格改连字符）
  - **无专用英文**：使用拼音规则（小写、无空格）
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页侧栏一致，仅第二层「地区列表」的 DOM 为「本地区： + dt/dd」，以便共用 CSS/JS。

---

## 港澳台「三级页面」右侧侧栏规则（区域页 / 市县页）

从港澳台二级页面点击进入**三级页面**后，**主内容区（body 左侧）与首页、港澳台二级页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为当前三级节点专用内容。港澳台三级页面分两类：

- **香港/澳门**：三级页为「区域页」（如香港岛、九龙、澳门半岛等）
- **台湾**：三级页为「市/县页」（如台北市、新竹县等）

### 1. 页面与主内容

- **页面路径**：
  - **香港**：`output/hong-kong/{区域英文路径}/index.html`
  - **澳门**：`output/macau/{区域英文路径}/index.html`
  - **台湾**：`output/taiwan/{市县英文路径}/index.html`
- **主内容（左侧）**：与首页、港澳台二级页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../../index.html`；第二项为港澳台地区名称链接到 `../index.html`（如「香港特别行政区」「澳门特别行政区」「台湾省」）；第三项为**当前区域/市县名称**，纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页、港澳台二级页共用同一 head、foot 模板；三级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../css/`、`../../images/`、`../../js/`。

### 2. 右侧侧栏整体结构

与首页、港澳台二级页侧栏**结构一致**，无「当前层级说明」块（不展示 `.sidebar-current` 内容）：

| 层级 | 说明 | 与港澳台二级页异同 |
|------|------|------------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示「本地区 + dt + dd」，改为「本区域/本市县全称 + 仅 dd 下级列表」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前三级节点**全称**：
  - 香港/澳门：当前**区域名称**（如「香港岛：」「九龙：」「澳门半岛：」等）
  - 台湾：当前**市/县名称**（如「台北市：」「新竹县：」等）
- **`.address-item-right`（内容）**：仅 **dd** 层级——当前三级节点下的下级列表（多个用 ` | ` 分隔）；**无 dt**：
  - **香港区域页（下级为区）**：`<dd><a href="{区英文路径}/index.html">{区名称}</a> | …</dd>`
  - **澳门区域页（下级为堂区）**：`<dd><a href="{堂区英文路径}/index.html">{堂区名称}</a> | …</dd>`
  - **台湾市/县页（下级为区/乡镇）**：`<dd><a href="{区/乡镇英文或拼音路径}/index.html">{区/乡镇名称}</a> | …</dd>`
- 数据顺序与 `HK-MO-TW.json` 中该节点下列表顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本节点： + dd 下级」内容；链接均为 `{下级路径}/index.html`，不按产品区分后缀。

### 4. 与港澳台二级页侧栏的对比小结

| 项目 | 港澳台二级页右侧侧栏 | 港澳台三级页右侧侧栏 |
|------|----------------------|----------------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无** |
| 地区内容 | 1 块：本地区全称 + dt 区域/市县、dd 下级 | **1 块**：本区域/本市县全称 + **仅 dd 下级** |
| 链接层级 | 区域/市县（dt）+ 区/堂区/区乡镇（dd） | **仅区/堂区/区乡镇**（dd） |
| 链接路径 | 区域/市县：`{区域/市县英文路径}/index.html`；下级：`{区域/市县英文路径}/{下级路径}/index.html` | 下级：`{下级路径}/index.html`（相对当前三级页） |

### 5. 实现要点

- 侧栏数据依赖**当前三级节点**及其在 `HK-MO-TW.json` 中的**下级列表**：
  - 香港：当前「区域」下的区列表
  - 澳门：当前「区域」下的堂区列表
  - 台湾：当前「市/县」下的区/乡镇列表
- path_slug 规则：
  - **区域/区/堂区/市县**：优先使用 `HK-MO-TW_name_mapping.json` 中的 `path_slug`
  - **台湾区/乡镇**：若无专用英文则用拼音（小写、无空格）
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页一致，仅第二层「地区列表」DOM 为「本节点： + dd」（单层 dd，无 dt），以便共用 CSS/JS。
- 三级页面为**三级页面**（首页 → 港澳台二级页 → 三级页），资源路径相对 output 根目录为 `../../`。

---

## 港澳台「四级页面」右侧侧栏规则（区页 / 堂区页 / 区乡镇页）

从港澳台三级页面点击进入**四级页面**后，**主内容区（body 左侧）与首页、港澳台二级页、三级页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**沿用三级页的「本区域/本市县 + dd 下级」内容，便于在同一上级区域内跳转。港澳台四级页面分三类：

- **香港**：四级页为「区页」（如中西区、湾仔区等18区）
- **澳门**：四级页为「堂区页」（如大堂区、望德堂区等堂区）
- **台湾**：四级页为「区/乡镇页」（如大安区、竹北市等）

### 1. 页面与主内容

- **页面路径**：
  - **香港**：`output/hong-kong/{区域英文路径}/{区英文路径}/index.html`，如 `hong-kong/hong-kong-island/central-and-western/index.html`（香港特别行政区香港岛中西区）
  - **澳门**：`output/macau/{区域英文路径}/{堂区英文路径}/index.html`，如 `macau/macau-peninsula/se/index.html`（澳门特别行政区澳门半岛大堂区）
  - **台湾**：`output/taiwan/{市县英文路径}/{区/乡镇英文或拼音路径}/index.html`，如 `taiwan/taipei/daanqu/index.html`（台湾省台北市大安区）
- **主内容（左侧）**：与首页、港澳台二级页、三级页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：
  - 第一项「首页」链接到 `../../../../index.html`；
  - 第二项为港澳台地区名称链接到 `../../index.html`（如「香港特别行政区」「澳门特别行政区」「台湾省」）；
  - 第三项为上级区域/市县名称链接到 `../index.html`（如「香港岛」「澳门半岛」「台北市」）；
  - 第四项为**当前区/堂区/区乡镇名称**（如「中西区」「大堂区」「大安区」），纯文字、不链接，表示当前页。
  - 不展示「地区导航」。
- **Head / Foot**：与首页、港澳台二级页、三级页共用同一 head、foot 模板；四级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」），如 `../../../css/`、`../../../images/`、`../../../js/`。

### 2. 右侧侧栏整体结构

与港澳台三级页面侧栏**完全一致**，无「当前层级说明」块：

| 层级 | 说明 | 与港澳台三级页异同 |
|------|------|------------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **相同**：本区域/本市县全称 + dd 下级列表 |

四级节点（区/堂区/区乡镇）为叶子节点，无下级；侧栏**复用三级页的「本区域/本市县： + dd 下级」**结构，便于在同一上级区域内跳转。

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用当前**上级节点全称**：
  - 香港：当前**区域名称**（如「香港岛：」「九龙：」）
  - 澳门：当前**区域名称**（如「澳门半岛：」「澳门外岛：」）
  - 台湾：当前**市/县名称**（如「台北市：」「新竹县：」）
- **`.address-item-right`（内容）**：仅 **dd** 层级——当前上级节点下的下级列表（多个用 ` | ` 分隔）；**无 dt**：
  - **香港区页（下级为区）**：`<dd><a href="{区英文路径}/index.html">{区名称}</a> | …</dd>`
  - **澳门堂区页（下级为堂区）**：`<dd><a href="{堂区英文路径}/index.html">{堂区名称}</a> | …</dd>`
  - **台湾区/乡镇页（下级为区/乡镇）**：`<dd><a href="{区/乡镇英文或拼音路径}/index.html">{区/乡镇名称}</a> | …</dd>`
- 数据顺序与 `HK-MO-TW.json` 中该上级节点下列表顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本节点： + dd 下级」内容；链接均为 `{下级路径}/index.html`（相对当前上级目录）。

### 4. 与港澳台三级页侧栏的对比小结

| 项目 | 港澳台三级页右侧侧栏 | 港澳台四级页右侧侧栏 |
|------|----------------------|----------------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回 | 无 | **无** |
| 地区内容 | 1 块：本区域/本市县全称 + dd 下级 | **1 块**：本区域/本市县全称 + dd 下级，**与三级页相同** |
| 链接层级 | 仅区/堂区/区乡镇（dd） | **仅区/堂区/区乡镇**（dd），**同上** |
| 链接路径 | 下级：`{下级路径}/index.html`（相对三级页） | 下级：`{下级路径}/index.html`（相对上级目录，即 `../` 当前四级页） |

### 5. 实现要点

- 侧栏数据与港澳台三级页面相同，依赖**当前上级节点**及其在 `HK-MO-TW.json` 中的**下级列表**：
  - 香港：当前「区域」下的区列表
  - 澳门：当前「区域」下的堂区列表
  - 台湾：当前「市/县」下的区/乡镇列表
- path_slug 规则：
  - **区域/区/堂区/市县**：优先使用 `HK-MO-TW_name_mapping.json` 中的 `path_slug`
  - **台湾区/乡镇**：若无专用英文则用拼音（小写、无空格）
- 侧栏 HTML 的 class、`data-for`、产品列表容器与三级页一致，仅第二层「地区列表」DOM 为「本节点： + dd」（单层 dd，无 dt），以便共用 CSS/JS。
- 四级页面为**四级页面**（首页 → 港澳台二级页 → 三级页 → 四级页），资源路径相对 output 根目录为 `../../../`。

---

## 普通省份页面右侧侧栏规则

从首页点击进入**普通省份页面**（如 `output/hebei/index.html`、`output/guangdong/index.html`）后，**主内容区（body 左侧）与首页完全一致**（导航、轮播图、5 个产品 list-box），**仅右侧侧栏**为省份专用内容。以下为当前实现的普通省份页面右侧侧栏规则。

### 1. 页面与主内容

- **页面路径**：`output/{省拼音}/index.html`，如 `hebei/index.html`（河北省）、`guangdong/index.html`（广东省）。
- **主内容（左侧）**：与首页**完全一致**——导航、轮播图（banner）、云服务器 / 高防服务器 / 服务器托管 / IDC / 机柜 5 个 list-box（样式与文案同首页）。
- **面包屑（nav-list）**：第一项「首页」链接到 `../index.html`；第二项为**当前省份名称**（如「河北省」「广东省」），纯文字、不链接，表示当前页。不展示「地区导航」。
- **Head / Foot**：与首页共用同一 head、foot 模板；二级页面需使用相对上级路径引用 css/images/js（见「二级及以下页面资源路径」）。

### 2. 右侧侧栏整体结构

与首页侧栏**结构一致**，无「当前层级说明」块（不展示「当前：河北」「返回全国」等 `.sidebar-current` 内容）：

| 层级 | 说明 | 与首页异同 |
|------|------|------------|
| 第一层 | 产品切换（云服务器、高防、托管、IDC、机柜） | **相同**：文案、`data-for`、class 命名 |
| 第二层 | 地区列表（每个产品对应一套列表） | **不同**：不展示全国 9 组，改为「本省全称 + dt 市 / dd 区·街道」 |

### 3. 侧栏内容规划（第二层）

- **`.address-item-left`（标签）**：使用省份**全称**（带「省/自治区」），如「河北省：」「广东省：」「内蒙古自治区：」。
- **`.address-item-right`（内容）**：**模仿首页**的 `<dl>` 结构——**dt** 放市（市名 + 链接），**dd** 放该市下区/县（区/县名 + 链接，多个用 ` | ` 分隔），如有街道则继续展示街道：
  - **dt**：`<dt><a href="{市拼音}/index.html">{市名称}</a></dt>`（如石家庄市、唐山市…）
  - **dd**：`<dd><a href="{市拼音}/{区拼音}/index.html">{区名称}</a> | …</dd>`
  - 对于没有下级的市（如部分直筒子市），dd 可为空
- 数据顺序与 `pcas.json` 中该省「市列表」及各市下「区县列表」顺序一致。
- 每个产品（云服务器、高防、托管、IDC、机柜）下展示**同一套**「本省： + dt/dd 市·区县」内容；链接均为 `index.html`（市页、区县页），不按产品区分后缀。

### 4. 与首页侧栏的对比小结

| 项目 | 首页右侧侧栏 | 普通省份页右侧侧栏 |
|------|--------------|--------------------|
| 产品切换 | 5 个产品 | 5 个产品，**同上** |
| 当前层级/返回首页 | 无 | **无**（不展示 sidebar-current） |
| 地区内容 | 9 组：直辖市、港澳台、华北～西北 | **1 块**：本省全称（如河北省：）+ dt 市、dd 区县 |
| 链接层级 | 直辖市/港澳台为一级；七大区域为省+市 | **市**（dt）+ **区县**（dd） |
| 链接路径 | `{省/市/直辖市拼音}/{产品页}.html` 等 | 市：`{市拼音}/index.html`；区县：`{市拼音}/{区县拼音}/index.html` |

### 5. 实现要点

- 侧栏数据依赖**当前省份**及其在 `pcas.json` 中的**市列表 + 各市下区县列表**（得到 `cities_with_districts`：`[(市名称, 市拼音, [(区县名称, 区县拼音), …]), …]`）。
- 市名、区县名转 path_slug 与现有拼音规则一致（小写、无空格）。
- 侧栏 HTML 的 class、`data-for`、产品列表容器与首页侧栏一致，仅第二层「地区列表」的 DOM 为「本省： + dt/dd」，以便共用 CSS/JS。

---

## 技术实现要求

### 二级及以下页面资源路径

直辖市页、区页、街道页等**二级及以下页面**（如 `output/beijingshi/index.html`、`output/beijingshi/dongchengqu/index.html`）与首页共用同一套 css、images、js（位于 `output/` 根目录）。模板中统一使用 `./css/`、`./images/`、`./js/`；生成脚本在**写入前**根据页面相对 `output` 的层级，对 HTML 中的 `href="./`、`src="./`、`content="./` 做重写，改为 `../`、`../../`、`../../../` 等，使二级、三级、四级页面能正确加载资源。首页（`output/index.html`）不做重写。

### 依赖库
- `json`: 解析 JSON 数据
- `pypinyin`: 中文转拼音
- `os` / `pathlib`: 文件系统操作

### 模板文件位置
建议在 `cities/` 目录下创建模板文件夹：
```
cities/
  ├── pcas.json
  ├── templates/
  │   ├── head_template.html
  │   ├── body_province_template.html      # 省级别模板（普通省份）
  │   ├── body_municipality_template.html  # 直辖市级别模板
  │   ├── body_city_template.html         # 市级别模板
  │   ├── body_district_template.html     # 区级别模板
  │   ├── body_street_template.html        # 街道级别模板
  │   └── foot_template.html
  └── 需求说明.md
```

**注意**：如果某些层级使用相同的模板，可以复用同一个模板文件。

### 输出目录
- 默认输出目录：`cities/output/`
- 可配置输出路径

## 特殊处理

### 1. 直辖市特殊处理

**四个直辖市列表**：
- 北京市
- 天津市
- 上海市
- 重庆市

**处理规则**：
1. 识别四个直辖市：在遍历数据时，判断省份名称是否为上述四个直辖市之一
2. 跳过"市辖区"层：当遇到直辖市时，数据中的"市辖区"是占位符，需要跳过这一层，直接访问区县数据
3. 路径生成：直辖市的路径只有三层（市 -> 区 -> 街道），而普通省份有四层（省 -> 市 -> 区 -> 街道）
4. 变量替换：在模板中，直辖市的情况：
   - `{省份名称}` = 直辖市名称（如"北京市"）
   - `{城市名称}` = 直辖市名称（与省份名称相同）
   - `{区县名称}` = 区名称
   - `{街道名称}` = 街道名称

**实现示例**：
```python
# 四个直辖市列表
MUNICIPALITIES = ["北京市", "天津市", "上海市", "重庆市"]

# 判断是否为直辖市
is_municipality = province_name in MUNICIPALITIES

if is_municipality:
    # 跳过"市辖区"层，直接遍历区县
    for district_name, streets in city_data.items():
        # 生成路径：{直辖市拼音}/{区拼音}/{街道拼音}/index.html
        ...
else:
    # 普通省份，遍历城市
    for city_name, districts in province_data.items():
        # 生成路径：{省拼音}/{市拼音}/{区拼音}/{街道拼音}/index.html
        ...
```

### 2. 港澳台特殊处理

**命名原则**：香港、澳门、台湾**使用英文**（不用拼音）；下级区域**有专用英文则用英文，找不到专用英文则用拼音**。

**首页/索引页中的港澳台逻辑**（与模板 `templates_index/index.html` 一致）：

1. **分组位置**：在首页地区导航中，「港澳台」为独立分组，排在「直辖市」之后、「华北地区」等大陆区域之前。顺序为：
   - 直辖市 → **港澳台** → 华北地区 → 东北地区 → 华东地区 → 华中地区 → 华南地区 → 西南地区 → 西北地区

2. **港澳台三项（英文）**：固定展示三个地区，**使用英文名称与路径**：
   - **香港**：显示名 Hong Kong，路径/链接 `hong-kong`（或 `hongkong`，项目内统一即可）
   - **澳门**：显示名 Macau，路径/链接 `macau`（Macao 为另一拼法，择一统一）
   - **台湾**：显示名 Taiwan，路径/链接 `taiwan`

3. **链接与路径**：
   - 首页侧栏中每个产品（如云服务器、高防、托管、IDC、机柜等）下均有「港澳台」分组
   - 链接形式：`{地区英文路径}/{产品页}.html`，例如：
     - `hong-kong/cloud.html`、`macau/cloud.html`、`taiwan/cloud.html`
     - `hong-kong/ddos.html`、`macau/ddos.html`、`taiwan/ddos.html`
   - 生成索引时，港澳台区块的 HTML 结构可与直辖市一致（左侧「港澳台：」，右侧三个链接）。

4. **下级区域命名（有专用英文用英文，无则用拼音）**：

   **香港**（官方 18 区均有英文，建议全部使用英文；路径可用小写+连字符）：
   - 区域：香港岛 → **Hong Kong Island**，九龙 → **Kowloon**，新界 → **New Territories**
   - 18 区官方英文（路径建议：小写、空格改连字符）：Central and Western、Wan Chai、Eastern、Southern（香港岛）；Yau Tsim Mong、Sham Shui Po、Kowloon City、Wong Tai Sin、Kwun Tong（九龙）；Kwai Tsing、Tsuen Wan、Tuen Mun、Yuen Long、North、Tai Po、Sai Kung、Sha Tin、Islands（新界）

   **澳门**（堂区有葡文/英文专用名，建议使用）：
   - 区域：澳门半岛 → **Macau Peninsula**，澳门外岛（氹仔、路环）→ **Taipa and Coloane** 或 **Outer Islands**
   - 堂区英文（路径可用简短形式）：大堂区 Sé、望德堂区 São Lázaro、风顺堂区 São Lourenço、花地玛堂区 Nossa Senhora de Fátima（或 Fátima）、圣安多尼堂区 Santo António、嘉模堂区（氹仔）Taipa、圣方济各堂区（路环）Coloane

   **台湾**（市/县多有官方英文，区/乡镇多数无通用英文，用拼音）：
   - 市/县：台北市 Taipei、新北市 New Taipei、桃园市 Taoyuan、台中市 Taichung、台南市 Tainan、高雄市 Kaohsiung、基隆市 Keelung、新竹市 Hsinchu City、嘉义市 Chiayi City；新竹县 Hsinchu County、苗栗县 Miaoli、彰化县 Changhua、南投县 Nantou、云林县 Yunlin、嘉义县 Chiayi County、屏东县 Pingtung、宜兰县 Yilan、花莲县 Hualien、台东县 Taitung、澎湖县 Penghu 等（以政府/国际常用英文为准）
   - 区/乡镇：若查得到通用英文则用英文，否则用拼音（如 pypinyin 或 Wade–Giles 等统一罗马化）

5. **数据与层级**（若生成港澳台下级页面）：
   - 香港：输出到 `output/hong-kong/` 下，下级路径使用上述英文（如 `hong-kong-island/`、`central-and-western/` 等）
   - 澳门：输出到 `output/macau/` 下，下级路径使用上述英文或简短形式（如 `taipa`、`coloane`、`se`）
   - 台湾：输出到 `output/taiwan/` 下，市/县用英文路径，区/乡镇用英文或拼音
   - 若使用单独数据文件（如 `HK-MO-TW.json`），需在生成流程中合并或单独遍历，并做「中文名 → 英文/拼音」映射表（香港 18 区、澳门堂区、台湾县市等）

6. **实现要点**：
   - 首页索引生成时，港澳台三项**固定使用英文**：Hong Kong、Macau、Taiwan；路径 `hong-kong`、`macau`、`taiwan`。
   - 下级区域：维护「中文 → 英文」映射表（香港区、澳门堂区、台湾县市等）；表中无对应英文时，该级使用拼音。
   - **中英对照数据**：`cities/data/HK-MO-TW_name_mapping.json` 提供港澳台及下级区域的中-英对照（含 `name_en`、`path_slug`），供生成页面程序直接调用。

### 3. 同名处理
- 如果存在同名的街道（不同区县），需要确保文件路径唯一
- 建议在文件名中包含区县信息，或使用序号区分

### 4. 特殊字符处理
- 处理拼音转换中的特殊字符
- 处理文件系统不支持的字符（如 `/`, `\`, `:` 等）

### 5. 错误处理
- 处理 JSON 解析错误
- 处理文件写入错误
- 处理拼音转换失败的情况
- 记录错误日志

## 输出示例

### 文件夹结构示例

**直辖市示例（北京市）**：
```
output/
  └── beijingshi/
      ├── index.html                    # 北京市首页（列出所有区）
      └── dongchengqu/
          ├── index.html                # 东城区首页（列出所有街道）
          └── donghuamenjiedao/
              └── index.html            # 东华门街道首页
          └── jingshanjiedao/
              └── index.html            # 景山街道首页
          └── jiaodaokoujiedao/
              └── index.html            # 交道口街道首页
      └── xichengqu/
          ├── index.html                # 西城区首页（列出所有街道）
          └── xichanganjiejiedao/
              └── index.html            # 西长安街街道首页
          └── xinjiekoujiedao/
              └── index.html            # 新街口街道首页
```

**普通省份示例（河北省石家庄市）**：
```
output/
  └── hebeisheng/
      ├── index.html                    # 河北省首页（列出所有市）
      └── shijiazhuangshi/
          ├── index.html                # 石家庄市首页（列出所有区）
          └── changanqu/
              ├── index.html            # 长安区首页（列出所有街道）
              └── jianbeijiedao/
                  └── index.html        # 建北街道首页
              └── qingyuanjiedao/
                  └── index.html        # 青园街道首页
          └── qiaoxiqu/
              ├── index.html            # 桥西区首页（列出所有街道）
              └── donglijiedao/
                  └── index.html        # 东里街道首页
```

### HTML 文件内容示例

#### 省级别/直辖市级别 HTML 示例

**直辖市示例（北京市首页 - beijingshi/index.html）**：

页面结构与首页一致：共用 head/foot；**左侧主内容**为导航（面包屑：首页、北京市）、轮播图（banner）、云服务器 / 高防 / 托管 / IDC / 机柜 5 个 list-box；**右侧侧栏**为直辖市专用：`.address-item-left` 为「北京市：」，`.address-item-right` 内为 `<dl>`，dt 放区（如 `<a href="dongchengqu/index.html">东城区</a>`），dd 放该区街道（如 `<a href="dongchengqu/donghuamenjiedao/index.html">东华门街道</a> | …`）。资源路径使用 `../` 引用 output 根目录的 css/images/js。

**普通省份示例（河北省首页 - hebeisheng/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>河北省 - 行政区划</title>
</head>
<body>
<div class="province">
    <h1>河北省</h1>
    <h2>下辖城市：</h2>
    <ul>
        <li><a href="shijiazhuangshi/index.html">石家庄市</a></li>
        <li><a href="tangshanshi/index.html">唐山市</a></li>
        <!-- 更多市... -->
    </ul>
</div>
</body>
</html>
```

#### 市级别 HTML 示例

**普通省份示例（石家庄市首页 - hebeisheng/shijiazhuangshi/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>石家庄市 - 河北省</title>
</head>
<body>
<div class="city">
    <h1>石家庄市</h1>
    <p>所属省份：<a href="../index.html">河北省</a></p>
    <h2>下辖区县：</h2>
    <ul>
        <li><a href="changanqu/index.html">长安区</a></li>
        <li><a href="qiaoxiqu/index.html">桥西区</a></li>
        <!-- 更多区... -->
    </ul>
</div>
</body>
</html>
```

#### 区级别 HTML 示例

**直辖市示例（东城区首页 - beijingshi/dongchengqu/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>东城区 - 北京市</title>
</head>
<body>
<div class="district">
    <h1>东城区</h1>
    <p>所属城市：<a href="../index.html">北京市</a></p>
    <h2>下辖街道：</h2>
    <ul>
        <li><a href="donghuamenjiedao/index.html">东华门街道</a></li>
        <li><a href="jingshanjiedao/index.html">景山街道</a></li>
        <li><a href="jiaodaokoujiedao/index.html">交道口街道</a></li>
        <!-- 更多街道... -->
    </ul>
</div>
</body>
</html>
```

**普通省份示例（长安区首页 - hebeisheng/shijiazhuangshi/changanqu/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>长安区 - 石家庄市</title>
</head>
<body>
<div class="district">
    <h1>长安区</h1>
    <p>所属城市：<a href="../index.html">石家庄市</a></p>
    <p>所属省份：<a href="../../index.html">河北省</a></p>
    <h2>下辖街道：</h2>
    <ul>
        <li><a href="jianbeijiedao/index.html">建北街道</a></li>
        <li><a href="qingyuanjiedao/index.html">青园街道</a></li>
        <!-- 更多街道... -->
    </ul>
</div>
</body>
</html>
```

#### 街道级别 HTML 示例

**直辖市示例（东华门街道 - beijingshi/dongchengqu/donghuamenjiedao/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>东华门街道 - 东城区 - 北京市</title>
</head>
<body>
<div class="street">
    <h1>东华门街道</h1>
    <p>所属区县：<a href="../index.html">东城区</a></p>
    <p>所属城市：<a href="../../index.html">北京市</a></p>
    <p>所属省份：<a href="../../index.html">北京市</a></p>
</div>
</body>
</html>
```

**普通省份示例（建北街道 - hebeisheng/shijiazhuangshi/changanqu/jianbeijiedao/index.html）**：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>建北街道 - 长安区 - 石家庄市</title>
</head>
<body>
<div class="street">
    <h1>建北街道</h1>
    <p>所属区县：<a href="../index.html">长安区</a></p>
    <p>所属城市：<a href="../../index.html">石家庄市</a></p>
    <p>所属省份：<a href="../../../index.html">河北省</a></p>
</div>
</body>
</html>
```

**注意**：
- 对于直辖市，`{城市名称}` 和 `{省份名称}` 的值相同，都是直辖市名称
- 每个层级的 HTML 文件都应该包含导航链接，方便用户在不同层级间跳转
- `{下级列表}` 变量需要根据实际数据动态生成列表内容

## 配置选项

建议支持以下配置：
- 输出目录路径
- 模板文件路径
- 拼音风格（如：带声调、不带声调、首字母等）
- 是否生成索引文件
- 日志级别

## 后续扩展

1. **索引生成**: 生成总索引 HTML 文件，列出所有城市、区县、街道
2. **数据验证**: 验证生成的文件数量和结构
3. **增量更新**: 支持只更新变化的数据
4. **批量处理**: 支持多线程/多进程处理大量数据
5. **统计报告**: 生成处理统计报告（总数量、成功/失败数量等）

## 注意事项

1. 确保有足够的磁盘空间存储生成的 HTML 文件
2. 处理大量数据时注意内存使用
3. 考虑文件系统的路径长度限制
4. 建议先在小数据集上测试，确认逻辑正确后再处理完整数据

